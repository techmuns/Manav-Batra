# Munshot stock-search API bearer token.
# Same value as the other Munshot dashboards; set on the Cloudflare worker env
# (wrangler secrets) for deployed environments, and in a local .env for dev.
MUNS_ACCESS_TOKEN=
